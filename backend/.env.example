# Copiar este archivo como .env y completar con tus datos reales
# IMPORTANTE: el archivo .env NUNCA se sube a Git (ya esta en .gitignore)

DATABASE_URL="postgresql://usuario:password@localhost:5432/tienda_barrio?schema=public"
JWT_SECRET="cambia_esto_por_un_secreto_largo_y_seguro"
PORT=4000

